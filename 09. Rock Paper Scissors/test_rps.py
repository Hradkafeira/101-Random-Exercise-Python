from rps import rps

def test_rps():
    actual = rps(1)
    expected = str
    assert type(actual) == expected

def test_rps2():
    actual = rps(3)
    expected = str
    assert type(actual) == expected